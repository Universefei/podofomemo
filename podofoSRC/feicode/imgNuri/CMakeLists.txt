ADD_EXECUTABLE(imgNuri imgNuri.cpp ImageConverter.cpp)

TARGET_LINK_LIBRARIES(imgNuri ${PODOFO_LIB})
SET_TARGET_PROPERTIES(imgNuri PROPERTIES COMPILE_FLAGS "${PODOFO_CFLAGS}")
ADD_DEPENDENCIES(imgNuri ${PODOFO_DEPEND_TARGET})

INSTALL(TARGETS imgNuri
	RUNTIME DESTINATION "feicode")

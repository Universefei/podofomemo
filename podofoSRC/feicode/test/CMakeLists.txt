ADD_EXECUTABLE(test test.cpp ImageConverter.cpp)

TARGET_LINK_LIBRARIES(test ${PODOFO_LIB})
SET_TARGET_PROPERTIES(test PROPERTIES COMPILE_FLAGS "${PODOFO_CFLAGS}")
ADD_DEPENDENCIES(test ${PODOFO_DEPEND_TARGET})

INSTALL(TARGETS test
	RUNTIME DESTINATION "feicode")

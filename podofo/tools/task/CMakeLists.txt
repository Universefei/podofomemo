ADD_EXECUTABLE(task task.cpp ImageConverter.cpp)

TARGET_LINK_LIBRARIES(task ${PODOFO_LIB})
SET_TARGET_PROPERTIES(task PROPERTIES COMPILE_FLAGS "${PODOFO_CFLAGS}")
ADD_DEPENDENCIES(task ${PODOFO_DEPEND_TARGET})

INSTALL(TARGETS task
	RUNTIME DESTINATION "bin")

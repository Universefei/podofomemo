ADD_EXECUTABLE(TxPicGeo TxPicGeo.cpp)

TARGET_LINK_LIBRARIES(TxPicGeo ${PODOFO_LIB})
SET_TARGET_PROPERTIES(TxPicGeo PROPERTIES COMPILE_FLAGS "${PODOFO_CFLAGS}")
ADD_DEPENDENCIES(TxPicGeo ${PODOFO_DEPEND_TARGET})

INSTALL(TARGETS TxPicGeo
	RUNTIME DESTINATION "feicode")
